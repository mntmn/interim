(do
(def c 0)
(while 1
  (do
    (def c (+ 1 (% (+ c 1) 256)))
    (def b 320)
    (while (def b (- b 1))
      (do
        (def a 320)
        (while (def a (- a 1))
          (pixel a b (/ (+ a b) c)))))
    (flip))))























